Downloads sorter is created to make sorting files easier from downloads on a local machine. It does this by auto_run.py running in the background and any file that is downloaded
and sent to the downloads folder will trigger organize_downloads.py. This will be put into the appropriate folder based on extension type. 

